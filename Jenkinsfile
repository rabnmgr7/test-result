pipeline {
    agent any
    stages {
        stage('ImgBuild') {
            steps {
                sh '''echo "Building docker images"
                docker 
                '''
            }
        }
    }
}